"""
AI-Agentic Framework (HLD Section 5).

Agents operate on metadata and operational telemetry only. Business
transformations remain deterministic and metadata-driven — agents are
strictly advisory/operational (validation assistance, schema-drift
detection, error classification, self-healing orchestration,
documentation generation, and observability), never a substitute for
the deterministic transformation/validation/load engines in `core/`.
"""
